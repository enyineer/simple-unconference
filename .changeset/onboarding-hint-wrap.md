---
"simple-unconference": patch
---

Fixed the agenda setup checklist on mobile: text-only action hints (like "Open an unconference slot to place sessions") now sit below the step description instead of squeezing it into a one-word-wide column.
