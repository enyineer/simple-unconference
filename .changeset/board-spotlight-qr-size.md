---
"simple-unconference": patch
---

Double the size of the QR code on the board's pitch-mode spotlight overlay (and scale its caption with it) so it can actually be scanned from across a projected room.
